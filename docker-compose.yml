version: '3.8'

services:
  bot:
    build: .
    container_name: fcbarcelona_bet_bot
    volumes:
      - .:/app
      - bot_data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////app/data/bot.db
    restart: always

volumes:
  bot_data:
